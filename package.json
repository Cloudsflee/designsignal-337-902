{
  "name": "designsignal-337-902",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Evidence-first daily design research signal for ZJU 337/902 preparation",
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test",
    "doctor": "node src/cli.mjs doctor",
    "daily:fixture": "node src/cli.mjs daily --fixture --dry-run --json",
    "start": "node src/cli.mjs serve"
  }
}
